#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

echo "[seed_local] placeholder seed workflow."
echo "[seed_local] create canonical seed files under data/seed before enabling import."
